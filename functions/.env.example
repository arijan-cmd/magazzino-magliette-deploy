# Credenziali SMTP Aruba per la Cloud Function sendSaleMail
# Copia questo file in functions/.env e compila con i valori reali (non committare .env).
SMTP_HOST=smtps.aruba.it
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=true
SMTP_FROM=
SMTP_TO=
